---
title: A complete AI coding workflow
description: Small Go CLIs that compose into one loop — spec → parallel agent sessions → ship.
template: splash
hero:
  tagline: |
    Open-source AI coding toolkit by <a href="https://github.com/sahil87">@sahil87</a>, CTO of <a href="https://noon.design">Noon</a>. Used daily at Noon. No SDK, no vendor lock-in, no opinions you can't override.
  actions:
    - text: Install everything
      link: '#install'
      icon: right-arrow
      variant: primary
    - text: Browse the tools
      link: /tools/
      icon: external
      variant: secondary
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import Mermaid from '../../components/Mermaid.astro';

## The loop

One idea fans out into many parallel agent sessions — then converges into a single dashboard you can watch from your phone.

<Mermaid code={`flowchart TB
    idea["idea"] --> fab["fab-kit"]
    fab --> w1["wt + agent #1"]
    fab --> w2["wt + agent #2"]
    fab --> w3["wt + agent #3"]
    w1 --> rk["run-kit"]
    w2 --> rk
    w3 --> rk
    rk --> ship(["ship"])

    classDef tool stroke:#3b82f6,stroke-width:2px;
    classDef agent stroke:#10b981,stroke-width:2px;
    classDef ship stroke:#f97316,stroke-width:2px;
    class idea,fab,rk tool;
    class w1,w2,w3 agent;
    class ship ship;`} />

**The loop:** `idea` captures it · `fab-kit` turns it into a spec the AI can't fudge · `wt` spins up an isolated worktree per change so N agents work in parallel without conflicts · `run-kit` gives you one browser tab to watch them all (mobile-friendly via Tailscale).

<details>
<summary>The full picture — how `hop` and `tu` wrap the loop</summary>

<Mermaid code={`flowchart TB
    hop["hop"] -.->|navigate| loop[["the loop above"]]
    loop -.->|tokens| tu["tu"]

    classDef ambient stroke:#a855f7,stroke-width:2px;
    classDef loopnode stroke:#3b82f6,stroke-width:2px;
    class hop,tu ambient;
    class loop loopnode;`} />

**Wrapping it:** `hop` jumps you into any worktree (or across repos) in one keystroke · `tu` tracks what all those parallel agents are costing you in tokens · `shll` keeps everything installed and updated.

</details>

## Install [#install]

```bash
brew install sahil87/tap/all   # installs every tool in the toolkit
shll shell-install             # one command to wire shell integration into your rc file
exec $SHELL                    # reload so the integration takes effect
```

Later, `shll update` upgrades every installed tool in one go. See the [shll README](https://github.com/sahil87/shll) for the full command surface.

## Tools

<CardGrid>
  <Card title="idea" icon="pencil">
    Plain-Markdown backlog (`fab/backlog.md`) — worktree-aware, queryable from the CLI, feeds `/fab-new`.<br/>
    [Docs →](/tools/idea/) · [GitHub →](https://github.com/sahil87/idea)
  </Card>
  <Card title="hop" icon="rocket">
    Fuzzy-nav, batch-git, and run-anything-inside-any-repo from one `hop.yaml` config.<br/>
    [Docs →](/tools/hop/) · [GitHub →](https://github.com/sahil87/hop)
  </Card>
  <Card title="fab-kit" icon="setting">
    7-stage pipeline that forces AI agents to plan before they code.<br/>
    [Docs →](/tools/fab-kit/) · [GitHub →](https://github.com/sahil87/fab-kit)
  </Card>
  <Card title="wt" icon="seti:folder">
    Opinionated `git worktree` wrapper — one worktree per change, zero conflicts.<br/>
    [Docs →](/tools/wt/) · [GitHub →](https://github.com/sahil87/wt)
  </Card>
  <Card title="run-kit" icon="laptop">
    Browser dashboard for tmux + Claude Code workspaces. Mobile-friendly via Tailscale.<br/>
    [Docs →](/tools/run-kit/) · [GitHub →](https://github.com/sahil87/run-kit)
  </Card>
  <Card title="tu" icon="bars">
    Token/cost tracker for Claude Code, Codex, OpenCode. Multi-machine sync, live watch.<br/>
    [Docs →](/tools/tu/) · [GitHub →](https://github.com/sahil87/tu)
  </Card>
  <Card title="shll" icon="puzzle">
    Meta-CLI — `shll install / update / shell-install` to wire and maintain the whole toolkit.<br/>
    [Docs →](/tools/shll/) · [GitHub →](https://github.com/sahil87/shll)
  </Card>
</CardGrid>

## Community

Questions, feature requests, or just want to chat about AI dev tooling? **[Join the Discord](https://discord.gg/32XHh5mJYn)** — that's where I answer queries, share what I'm building, and discuss with people using these tools.
